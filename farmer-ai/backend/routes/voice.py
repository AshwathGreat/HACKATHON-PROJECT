"""
PHASE 6 STUB - not yet implemented, and NOT included in main.py yet.

Will hold the voice pipeline endpoint(s) that:
  farmer voice -> speech_to_text -> intent understanding -> backend logic
  -> verified answer -> localize_response -> text_to_speech -> WhatsApp voice reply

Implementation added in Phase 6 - see README.md
"""

from fastapi import APIRouter

router = APIRouter()
